# 3.17.1 → 3.18.4

结构扫描时间 2026-08-07。DEX 全量 CRC 变了，按组件/资源看，不是重写。

## 产品

1. **身份核验 / 人脸**  
   `com.idv.identity.*`（Toyger）、多枚 `.mnn`、`libface` / `libIDVMNN` / `libidentityutils`，域名 `cloudauth-intl*` / `authidv` / `yuncloudauth`。
2. **证券划转**  
   `com.plutus.stock.transfer.*`：Activity / Flow / Detail / Search / History。
3. **C2C 聊天**  
   建议联系人、群聊 @ 例外、`ChatSessionListV2`。
4. **小程序运行时**  
   `assets/common` **5.14.0 → 5.15.0**，登录小程序加 `reset-screen-pin`。
5. **安全对抗名单变长**  
   Manifest 里 Magisk / KernelSU / 虚拟摄像头等包名。
6. **法币 / 卡组资源**  
   `fiat_config_default.json`，OCBS / 卡组静态图 URL。

## 下线

- `PaymentTutorialActivity`
- `api.saasexch.com`

## 体积

+9.65 MB。native lib +8.3 MB（新人脸 so），assets +2.5 MB。
