# 3.19.8 彩蛋

## 真彩蛋

没有。没有 you found / secret menu / 点 Logo / 摇一摇解锁。

## 产品预告（这版仍在包里，不是新进）

- `Options (Coming soon)` — 3.17.1 起就有。这版期权链已经能打通，这句话还留着，灰度两态，和 3.19.5 的 `POV 订单（即将上线）` 同一类。

## 看起来像彩蛋、其实是旧文案或包装假差

| 东西 | 实际 |
|---|---|
| `Dina Degen` | 3.17.1 起就在 `classes3.dex` |
| 烟花 / to the moon / `我进场就像放烟花` | 旧 arsc 就有。这份 3.19.8 缺中文资源，扫不到不等于删了 |
| `Binance AI Pro is coming soon` | 3.17.1 |
| `Introducing Dual Identity in Chat` | 3.17.1 |
| `EggView` | 人脸 SDK 取景框 |
| 73 个 so / eKYC tflite / 人脸 mnn 「整组删除」 | 这份是不带 `lib/` 的 base APK，不是产品卸了核验 |
| `hahahahaha` | 调试 key，旧 |

## 没进这三个包

`Continue to X`、yellow cloth、under the cloth。黄布图只在站外 X，继续盯远程小程序 / CMS / `getUserAppFeatures` v3。

## 扫描误报

`US Eastern`、`clothing_store`、`curtains` 库、`org.meowcat.edxposed`、R8 哈希、域名 `lamborghini`、`keccak`。  
`Call` / `Put` / `Strike` 单字命中率高，不能当新功能。
