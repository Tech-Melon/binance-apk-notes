# 3.19.8 彩蛋

## 真彩蛋

没有。没有 you found / secret menu / 点 Logo / 摇一摇解锁。

## 产品预告（这版仍在包里，不是新进）

- `Options (Coming soon)` / `期权（即将上线）` — 3.17.1 起就有。这版期权链已经能打通，这句话还留着，灰度两态。

## 看起来像彩蛋、其实是旧文案或包装假差

| 东西 | 实际 |
|---|---|
| `Dina Degen` | 3.17.1 起就在 `classes3.dex` |
| 烟花 / to the moon / `我进场就像放烟花` | 3.17.1 起就有，完整 3.19.8 仍在 arsc |
| `Binance AI Pro is coming soon` | 3.17.1 |
| `Introducing Dual Identity in Chat` | 3.17.1 |
| `EggView` | 人脸 SDK 取景框 |
| Play base 缺 73 个 so / 缺中文 / 缺 eKYC 模型 | `币安 3.19.8.apk` 是不带 `lib/` 的拆分。完整 `BNApp64_V3.19.8.apk` 有 74 个 so、85 MB arsc |
| `hahahahaha` | 调试 key，旧 |

## 没进这三个包

`Continue to X`、yellow cloth、under the cloth。黄布图只在站外 X，继续盯远程小程序 / CMS / `getUserAppFeatures` v3。

## 扫描误报

`US Eastern`、`clothing_store`、`curtains` 库、`org.meowcat.edxposed`、R8 哈希、域名 `lamborghini`、`keccak`。  
`Call` / `Put` / `Strike` / `期权链` 单字或旧标签命中率高，不能当新功能。
